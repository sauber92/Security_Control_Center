<div class="modal-dialog modal-lg">
  <div class="modal-content">
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal">&times;</button>
      <h4 class="modal-title text-center">Secure Boot</h4>
    </div>
    <div class="modal-body info">
      <p style="margin-left: 30px">* 최신 로그가 상단에 표시됩니다</p>
      <table class="table table-hover table-bordered">
        <thead>
        <tr>
          <th style="text-align: center">로그</th>
          <th style="text-align: center">상태</th>
          <th style="text-align: center">업로드 시간</th>
        </tr>
        </thead>
        <tbody>
        <%
        for(var i = (count_secure_boot - 1); i >= 0; i--) {
        %>
        <tr>
          <td> <%= secure_boot[i].boot_log %> </td>
          <td> <%= secure_boot[i].boot_parity %> </td>
          <td> <%= secure_boot[i].boot_time %> </td>
        </tr>
        <%
        }
        %>
        </tbody>
      </table>
    </div>
    <div class="modal-footer">
      <button type="button" class="btn btn-default" data-dismiss="modal">뒤로가기</button>
    </div>
  </div>
</div>